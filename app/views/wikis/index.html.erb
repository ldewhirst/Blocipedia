<div class="container">
  <h2>Wikis</h2>
  <table class="table">
    <tbody>
      <td><%= @title %></td>

  <% if user_signed_in? %>
  <div class="col-md-4">
    <%= link_to "New Wiki", new_wiki_path, class: 'btn btn-success' %>
  </div>
  <% end %>
</div>
