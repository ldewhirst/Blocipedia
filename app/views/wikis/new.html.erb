<h1>New Wiki Submission</h1>
  <div class="row">
    <div class="col-md-8">

<%= form_for @wiki do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit "Save" %>
<% end %>

</div>
</div>
