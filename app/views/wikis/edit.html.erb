<h1>Edit Wiki</h1>
  <div class="row">
    <div class="col-md-4">

<%= form_for @wiki do |f| %>
<div class="field">
  <%= f.label :title %>
  <%= f.text_field :title %>
</div>

<div class="field">
  <%= f.label :body %>
  <%= f.text_area :body %>
</div>

<div class="field">
  <%= render :partial => 'form', :locals => {:f => f} %>
</div>

<%= render :partial => 'collaborators' %>

<div class="form-group">
  <%= f.submit "Save" %>
<% end %>

</div>
</div>
